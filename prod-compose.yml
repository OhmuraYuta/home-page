services:
  web:
    image: nginx:stable
    container_name: home_page
    expose:
      - "80"
    volumes:
      - ./html:/var/www/html
      - ./conf.d:/etc/nginx/conf.d
    restart: always
    networks:
      - proxy_network

networks:
  proxy_network:
    external: true